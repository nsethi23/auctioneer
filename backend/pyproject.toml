[project]
name = "auctioneer-backend"
version = "0.1.0"
description = "Simulation engine for ad auction mechanisms"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.8.0",
]

[tool.ruff]
line-length = 88
target-version = "py313"

